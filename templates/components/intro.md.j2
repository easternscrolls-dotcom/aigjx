{# 组件1：开篇。素材字段 + 短句库随机拼接，无 AI 生成 #}
{% if h2.h2_intro %}
## {{ h2.h2_intro }}
{% endif %}

{{ item.name }}

{{ transitions[0:1] | join('') }} For readers in {{ vars.region }}, availability and pricing can shift, so recheck the official page before you commit.

{% if keywords | length > 1 %}
*{{ keywords[1:4] | map('titlecase') | join(' · ') }}*
{% endif %}
