# AgentHub 零成本上线部署教程（零基础站长版）

> 全程一分钱不花。只用 **GitHub（存代码）+ Cloudflare（免费托管）**。
> 不买域名、不买服务器、不买任何付费接口。
> 本教程假设你完全没用过这些工具，跟着点就行。

---

## 开始前先知道 3 件事

1. **GitHub** = 免费网盘（帮你存网站源码，还能每天自动更新）。
2. **Cloudflare Pages** = 免费服务器（把你的代码变成别人能访问的网址，送你一个 `xxx.pages.dev` 免费二级域名）。
3. **智谱 AI / Firecrawl** = 给"自动流水线"用的免费钥匙，你不填也能先上线，只是不会"自动加新工具"。

---

## 第 1 步：创建 GitHub 仓库并上传源码

### 1.1 注册 GitHub（已有账号跳过）
- 打开 https://github.com
- 点右上角 **Sign up**，用邮箱注册（免费）。

### 1.2 新建仓库
- 登录后，点右上角 **+** → **New repository**。
- 填写：
  - **Repository name（仓库名）**：`agenthub`（随便起，英文小写）
  - **Description（描述）**：`AI智能体Agent工具导航站`
  - 选 **Public（公开）** ← 必须公开，否则 Cloudflare 免费版连不上
  - ✅ 勾选 **Add a README file**
- 点 **Create repository**。

### 1.3 把全站源码传上去
> 你已经有了前面生成的源码（`index.html`、`css/`、`js/`、`data/`、`*.html` 等）。把它们整包传上去即可。

**方法 A（最简单，零基础推荐）：网页拖拽上传**
1. 进入你的仓库页面。
2. 点 **Add file** → **Upload files**。
3. 把本地 `autoguide-station/` 里的**全部文件和文件夹**选中，直接拖进浏览器框。
4. 页面底部 **Commit changes** 写一句话：`首次上传全站源码`，点绿色按钮。

**方法 B（你本来就用 Git 推送，可跳过）**
```bash
# 在本地源码目录执行（换成你的仓库地址）
git remote add origin https://github.com/你的用户名/agenthub.git
git add -A
git commit -m "首次上传全站源码"
git push -u origin main
```

✅ 上传后，仓库里应该能看到：`index.html`、`local.html`、`css/`、`js/`、`data/`、`update.html`、`404.html`、`sitemap.xml` 等。

---

## 第 2 步：申请两个免费密钥（可选，但建议）

> 这两个密钥只用于"每天自动爬新工具"。**不申请也能上线网站**，只是网站不会自动更新。
> 全部是免费额度，按下面入口申请，别点任何"付费/Upgrade"按钮。

### 2.1 智谱 AI 免费密钥（GLM-4-Flash，永久免费模型）

1. 打开 https://open.bigmodel.cn （智谱开放平台，免费注册）。
2. 用手机号注册登录。
3. 右上角点头像 → **用户中心** → **API Keys**（或左侧"API密钥"）。
4. 点 **新建 API Key** → 随便写个名字（如 `agenthub`）→ 生成。
5. **复制那串 `xxxxxxxx-xxxx-xxxx` 的密钥**，存到记事本，第 3 步要用。

> 说明：本项目用的 `GLM-4-Flash` 是智谱的**免费模型**，有免费调用额度，长期可用，无需充值。

### 2.2 Firecrawl 免费密钥（每月 1000 次，够用）

1. 打开 https://www.firecrawl.dev
2. 点 **Sign up**（可用 GitHub 账号一键注册，免费）。
3. 进入 **Dashboard** → **API Keys**。
4. 点 **Create API Key** → 复制密钥存好。
5. 免费版每月 **1000 次** 抓取额度，本项目每天只用几次，完全够。

> 小提示：Firecrawl 是"可选"的增强采集源。不填它，流水线照样从 GitHub / HuggingFace 抓数据，不影响上线。

---

## 第 3 步：把密钥存进 GitHub（保密，不泄露）

> 密钥不能写进代码里（会泄露）。GitHub 提供"密码保险箱"（Secrets），只有程序能读，人看不到。

1. 打开你的 GitHub 仓库页面。
2. 点顶部 **Settings**（设置）。
3. 左侧栏找到 **Secrets and variables** → 点 **Actions**。
4. 点绿色的 **New repository secret**，逐个添加：

| Name（名称，照抄） | Secret value（值，粘贴你复制的密钥） |
|---|---|
| `ZHIPU_API_KEY` | 智谱的密钥（第 2.1 步复制的） |
| `FIRECRAWL_API_KEY` | Firecrawl 的密钥（第 2.2 步复制的，没有可先不填） |
| `CRAWL_TARGET_URL` | 留空即可（预留项，不填不影响） |

5. 每填一个就点 **Add secret**。

✅ 添加后列表里会显示名称（值被遮住，安全）。程序跑的时候自动读取，不公开。

---

## 第 4 步：Cloudflare Pages 一键部署（拿免费域名）

1. 打开 https://dash.cloudflare.com （用邮箱注册，免费）。
2. 左侧点 **Workers & Pages** → 右上角 **Create** → 选 **Pages** 标签。
3. 选 **Connect to Git**（连接 GitHub）。
4. 授权 Cloudflare 访问 GitHub（弹窗点 **Authorize** / **Install**）。
5. 在列表里选你刚才建的仓库 `agenthub`。
6. 设置构建（**这步照抄，别改错**）：
   - **Framework preset（框架）**：选 **None**（我们是纯静态，无框架）
   - **Build command（构建命令）**：**留空**（源码已是成品，无需打包）
   - **Build output directory（输出目录）**：填 `/`（一根斜杠，意思是根目录）
7. 点 **Save and Deploy**（保存并部署）。

⏳ 等 1～2 分钟，页面会显示一个网址，形如：
```
https://agenthub.pages.dev
```
这就是你的**免费二级域名**，全世界都能访问，不用买域名。

✅ 以后每次你往 GitHub 推代码，Cloudflare 会**自动重新部署**，网站自己更新。

---

## 第 5 步：开启 Cloudflare 免费优化（速度+安全）

全部免费，默认大多已开，照着确认一遍：

1. 进入 Cloudflare 控制台 → 选你的 `agenthub.pages.dev` 项目。
2. **Brotli 压缩（让网页加载更快）**：
   - 左侧 **Speed** → **Optimization** → 找到 **Brotli**，开关打开 ✅。
3. **全球 CDN（让世界各地都快）**：
   - Pages 默认就在 Cloudflare 全球节点上，无需设置，自动生效。
4. **HTTPS 加密（地址栏小锁）**：
   - 左侧 **SSL/TLS** → 模式选 **Automatic (HTTPS)**。免费证书自动签发。
   - 左侧 **SSL/TLS** → **Edge Certificates** → ✅ 打开 **Always Use HTTPS**（强制跳 https）。
5. **缓存规则（减少重复加载）**：
   - 本项目仓库里已有 `_headers` 文件，已写好转静态资源缓存规则，部署后自动生效，不用你动手。

✅ 这 4 项都免费，不开任何 "Pro / paid" 选项。

---

## 第 6 步：提交搜索引擎收录（让百度/谷歌能搜到）

> 不花一分钱，纯免费提交。目的是让用户在百度、谷歌搜"AI智能体工具"时能找到你。

### 6.1 谷歌 Search Console（推荐先做）
1. 打开 https://search.google.com/search-console/
2. 点 **添加资源** → 选 **网址前缀** → 填 `https://agenthub.pages.dev` → 继续。
3. 验证方式选 **HTML 标记**：复制那段 `<meta name="google-site-verification" ...>`。
4. 把它粘贴进你仓库的 `index.html` 的 `<head>` 里（在 `<title>` 下面加一行即可），推送到 GitHub。
5. Cloudflare 自动重新部署后，回 Search Console 点 **验证**。
6. 验证通过后，左侧 **站点地图** → 填 `https://agenthub.pages.dev/sitemap.xml` → 提交。

### 6.2 百度站长平台
1. 打开 https://ziyuan.baidu.com
2. 用百度账号登录 → **站点管理** → **添加网站** → 填 `https://agenthub.pages.dev`。
3. 验证方式同样选 **HTML 标签验证**：复制 `meta` 标签，加进 `index.html` 的 `<head>`（和谷歌那段放一起）。
4. 推送代码、重新部署后点 **完成验证**。
5. 左侧 **链接提交** → **自动提交 / sitemap** → 提交 `https://agenthub.pages.dev/sitemap.xml`。

> 提示：百度对免费 `pages.dev` 域名收录慢一些（可能几周到一个月），属正常，坚持更新内容即可。

---

## 第 7 步：每月 5 分钟日常维护（检查灰色失效工具）

网站会自动把"打不开的链接"标灰（前端不显示），数据里留着但不害人。每个月花 5 分钟清理一次：

1. 进 GitHub 仓库 → 顶部 **Actions** 标签。
2. 看最近一次 `Daily Crawl` 运行记录，点进去下载 **Artifacts** 里的 `crawl-log`（这就是每月检查用的执行日志）。
3. 打开日志，搜关键词 `失效` / `dead` / `链接校验失败`，记下哪些工具链接挂了。
4. 回到仓库，打开 `data/agent-list.json`。
5. 找到对应工具，把整段 `{ ... }` 删掉（或把 `"status": "dead"` 改成确认失效后删除）。
6. 页面底部 **Commit changes** → 推送 → Cloudflare 自动重新部署，灰色工具消失。

✅ 每月一次，5 分钟。不删也不会出错，只是列表里多一条灰的。

---

## 第 8 步：流量 & SEO 优化方案（不花一分钱推广）

本站的 SEO 底子已经打好，你只需要"持续加内容"：

1. **长尾词已内置**：每个页面 `keywords` 都写了国内搜索长尾词（如"本地开源智能体框架""跨境电商Agent""外贸邮件自动回复"）。**不要删改这些 meta 标签**。
2. **自动加新工具 = 自动涨 SEO**：每天定时流水线从 GitHub / HuggingFace 抓新 Agent 工具，新增页面自带独立 `title` / `description` / `keywords`，搜索引擎慢慢就收录更多词。
3. **坚持每月更新**（见第 7 步），搜索引擎喜欢"活着的站"。
4. **不买任何广告**：纯靠内容 + sitemap 提交，零成本获自然搜索流量。
5. **可顺手做的免费传播**（可选）：把站点链接发到相关免费社区、论坛签名、社交媒体简介——全是免费的。

---

## 常见避坑（照着做就不会错）

| 现象 | 原因 | 解决 |
|---|---|---|
| 网站打不开 / 404 | Cloudflare 输出目录填错 | 第 4 步输出目录必须是 `/` |
| 卡片不显示 | 直接双击 `index.html` 打开（本地 file:// 会被浏览器拦） | 必须用本地服务器看，或等 Cloudflare 上线后看 `https://xxx.pages.dev` |
| 每天没自动加新工具 | 没填 `ZHIPU_API_KEY` | 第 2、3 步补密钥；不填也能正常运行，只是不走 AI 清洗 |
| 百度一直不收录 | pages.dev 免费域名收录慢 | 正常现象，持续更新 + 提交 sitemap 即可 |
| 推送后没更新 | Cloudflare 没重新部署 | 进 Cloudflare 项目点 **Deployments** 看是否成功；失败看构建日志 |

---

## 一句话总结

**GitHub 存代码 → Cloudflare 免费托管拿 `xxx.pages.dev` → 填两个免费密钥（可选）→ 提交搜索引擎 → 每月 5 分钟清灰色工具。**
全程零成本，关机也自动跑，不用买任何东西。
