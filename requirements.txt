# ============================================================
# autoguide-station 依赖清单
# 全部为开源免费库，无任何付费 SDK / 付费 API 客户端
# 安装：pip install -r requirements.txt
# ============================================================

requests>=2.31.0          # HTTP 请求（翻译源、挖词、RSS 兜底抓取）
PyYAML>=6.0.1             # 读取 config/*.yaml
Jinja2>=3.1.3             # 模块化内容模板引擎（无AI，纯模板拼接）
feedparser>=6.0.11        # RSS/Atom 解析
beautifulsoup4>=4.12.3    # HTML 清洗与相关搜索词解析
Pillow>=10.0.0            # 生成社交分享 PNG 横幅（og:image，零系统级 cairo 依赖）

# ------------------------------------------------------------
# 可选：py-googletrans
# translate.py 会优先 try import googletrans，导入失败时自动降级为
# 内置的 translate.googleapis.com 免密钥端点（与 googletrans 同源）。
# 该库在部分环境与新版 httpx 冲突，因此默认注释，按需开启。
# ------------------------------------------------------------
# googletrans==4.0.0rc1
